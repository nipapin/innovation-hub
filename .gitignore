# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store
.env

# Local backup of the original (uncompressed) promo video
promo_video_original_backup.mp4
public/promo_video_compressed.mp4

# typescript
*.tsbuildinfo
next-env.d.ts

# Снимок схемы базы: генерируется из живого сервера, не исходник.
# Пересобрать — npm run db:schema-doc (нужен доступ к базе).
docs/DB_SCHEMA.html
