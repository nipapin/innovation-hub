/**
 * Сколько дней удалённое лежит в корзине, прежде чем стереться насовсем.
 *
 * Отдельный модуль ради одного числа: `lib/storage/trash.ts` тянет за собой
 * AWS SDK и `pg`, а срок надо показать на карточке проекта в корзине — то есть
 * в клиентском компоненте. Импорт оттуда утащил бы серверные зависимости в
 * браузерный бандл, а дублировать число в двух местах значило бы однажды
 * поменять его в одном.
 */
export const TRASH_RETENTION_DAYS = 30
